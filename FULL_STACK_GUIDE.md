# ChainMonitor 全栈运行指南

完整的端到端运行流程，包括智能合约、后端监控、API服务器、数据库和前端。

## 📋 架构概览

```
┌─────────────┐      ┌──────────────┐      ┌─────────────┐
│  以太坊主网  │ ───> │ 后端监控服务  │ ───> │ PostgreSQL  │
│ (数据源)    │      │  monitor.py  │      │   数据库    │
└─────────────┘      └──────────────┘      └─────────────┘
                             │                     │
                             ↓                     ↓
                     ┌──────────────┐      ┌─────────────┐
                     │ Sepolia测试网│      │ API Server  │
                     │ 智能合约     │      │   FastAPI   │
                     └──────────────┘      └─────────────┘
                             ↑                     ↑
                             │                     │
                     ┌───────────────────────────────┐
                     │      React 前端应用           │
                     │  (读合约 + 调用API)          │
                     └───────────────────────────────┘
```

## 🚀 第一步：环境准备

### 1.1 安装依赖

```bash
# Node.js 依赖（用于合约部署和前端）
npm install

# Python 依赖（用于后端）
cd backend
pip install -r requirements.txt
cd ..

# 数据库依赖
cd database
pip install -r requirements.txt
cd ..
```

### 1.2 配置环境变量

创建项目根目录的 `.env` 文件：

```bash
# 复制模板
cp .env.docker .env
```

编辑 `.env` 文件，填写以下配置：

```env
# ======================================
# 1. 区块链RPC节点
# ======================================
# Sepolia测试网RPC（用于部署合约）
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY

# 以太坊主网RPC（用于抓取真实数据）
ETH_RPC_URL=https://mainnet.infura.io/v3/YOUR_INFURA_KEY

# ======================================
# 2. 钱包私钥（测试网）
# ======================================
# 警告：不要使用主网私钥！仅用于Sepolia测试网
PRIVATE_KEY=0x...your_test_wallet_private_key

# ======================================
# 3. 智能合约地址（部署后填写）
# ======================================
CONTRACT_ADDRESS=  # 先留空，部署后填写

# ======================================
# 4. 数据库配置
# ======================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chainmonitor
DB_USER=chainmonitor_user
DB_PASSWORD=chainmonitor_pass

# ======================================
# 5. API服务器配置
# ======================================
API_PORT=8000
```

## 🔧 第二步：部署智能合约（5分钟）

### 2.1 编译合约

```bash
npx hardhat compile
```

### 2.2 部署到Sepolia测试网

```bash
npm run deploy:sepolia
```

**预期输出：**
```
正在部署 RiskMonitor 合约...
✅ RiskMonitor 已部署到: 0x1234567890abcdef...
```

### 2.3 更新环境变量

将输出的合约地址复制到 `.env` 文件的 `CONTRACT_ADDRESS` 字段：

```env
CONTRACT_ADDRESS=0x1234567890abcdef...
```

### 2.4 验证合约（可选）

在 [Sepolia Etherscan](https://sepolia.etherscan.io/) 搜索合约地址，确认部署成功。

## 💾 第三步：启动数据库（2分钟）

### 方式A：使用Docker（推荐）

```bash
# 启动PostgreSQL容器
docker-compose up -d

# 等待5秒让数据库启动
sleep 5

# 初始化schema
docker exec -i chainmonitor-db psql -U chainmonitor_user -d chainmonitor < database/migrations/001_init_schema.sql

# 生成测试数据
cd database
python3 seeds/generate_test_data.py
cd ..
```

### 方式B：使用本地PostgreSQL

```bash
cd database
./setup.sh
cd ..
```

### 验证数据库

```bash
# 检查表是否创建成功
docker exec -it chainmonitor-db psql -U chainmonitor_user -d chainmonitor -c "\dt"

# 应该看到10张表：
# markets, risk_levels, risk_factors, dex_transactions,
# whale_transactions, cex_flows, liquidity_snapshots,
# alerts, user_configs, monitoring_stats
```

## 🖥️ 第四步：启动API服务器（常驻运行）

API服务器为前端提供数据接口。

### 4.1 启动API服务器

打开**第一个终端窗口**：

```bash
cd backend
python3 api_server.py
```

**预期输出：**
```
🚀 启动 ChainMonitor API Server on http://localhost:8000
📚 API文档: http://localhost:8000/docs
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### 4.2 测试API

在浏览器打开 http://localhost:8000/docs 查看API文档。

或使用curl测试：

```bash
# 健康检查
curl http://localhost:8000/api/health

# 获取市场列表
curl http://localhost:8000/api/markets
```

**保持这个终端运行！**

## 🔍 第五步：启动后端监控服务（常驻运行）

后端监控服务从以太坊主网抓取数据，计算风险，写入数据库和合约。

### 5.1 配置监控市场

编辑 `backend/markets.json`，配置要监控的市场：

```json
[
  {
    "type": "dex_pool",
    "label": "UNISWAP_USDC_WETH",
    "pairAddress": "0xB4e16d0168e52d35CaCD2c6185b44281Ec28C9Dc",
    "description": "Uniswap V2 USDC/WETH池"
  },
  {
    "type": "whale",
    "address": "0x...",
    "description": "某巨鲸地址"
  },
  {
    "type": "exchange",
    "address": "0x...",
    "description": "Binance热钱包"
  }
]
```

### 5.2 启动监控服务

打开**第二个终端窗口**：

```bash
cd backend
python3 monitor.py
```

**预期输出：**
```
🚀 启动监控：
  监控市场 label      : UNISWAP_USDC_WETH
  DEX 池子地址        : 0xB4e16d0168e52d35CaCD2c6185b44281Ec28C9Dc
  marketId(bytes32)   : 0x1234...
  巨鲸地址数          : 5
  交易所热钱包地址数  : 3

=== 开始新一轮监控 ===
✅ 已连接 mainnet, 最新区块: 18945678
获取到 245 笔交易
DEX 交易笔数: 245, volume: 1234567890
巨鲸卖出总量: 5000000, 卖出巨鲸数: 2
📊 综合风险评分: 45 (dex=20, whale=15, cex=10)
当前计算风险等级: 1
⚠️ 风险等级从 None 变为 1，调用合约更新...
📨 发送 updateRisk 交易: 0xabcd...
⏳ 等待 60 秒后进行下一轮...
```

监控服务会：
- 每60秒抓取一次主网数据
- 计算风险评分（0-100）
- 映射到风险等级（0-3）
- 写入PostgreSQL数据库
- 如果等级变化，自动调用Sepolia合约更新

**保持这个终端运行！**

## 🎨 第六步：启动前端应用（开发模式）

### 6.1 配置前端环境变量

编辑 `frontend/.env`：

```env
# Sepolia测试网RPC
VITE_SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY

# 智能合约地址（与.env根目录一致）
VITE_CONTRACT_ADDRESS=0x1234567890abcdef...

# API服务器地址
VITE_API_BASE_URL=http://localhost:8000/api

# 链ID（Sepolia = 11155111）
VITE_CHAIN_ID=11155111
```

### 6.2 启动前端开发服务器

打开**第三个终端窗口**：

```bash
cd frontend
npm run dev
```

**预期输出：**
```
  VITE v5.0.11  ready in 1234 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

### 6.3 访问前端

在浏览器打开 **http://localhost:5173**

你应该看到：
- ✅ Dashboard页面展示所有市场
- ✅ 实时风险等级和评分（来自数据库）
- ✅ 风险趋势图表
- ✅ 交易记录列表

### 6.4 连接钱包（可选）

点击右上角"Connect Wallet"：
1. 选择MetaMask
2. 切换到Sepolia测试网
3. 连接钱包后可以：
   - 查看合约中的风险等级
   - 配置告警阈值
   - 查看链上事件

## 📊 第七步：验证完整流程

### 7.1 数据流验证

1. **后端监控** → **数据库**
   ```bash
   # 查看最新的风险记录
   docker exec -it chainmonitor-db psql -U chainmonitor_user -d chainmonitor \
     -c "SELECT market_id, risk_level, risk_score, created_at FROM risk_levels ORDER BY created_at DESC LIMIT 5;"
   ```

2. **数据库** → **API** → **前端**
   - 打开前端Dashboard
   - 应该看到市场列表和风险数据
   - 数据来自PostgreSQL，通过API提供

3. **后端监控** → **智能合约**
   ```bash
   # 在Sepolia Etherscan查看合约交易
   # https://sepolia.etherscan.io/address/YOUR_CONTRACT_ADDRESS
   ```

4. **智能合约** → **前端**
   - 连接钱包后
   - 前端会直接读取合约中的风险等级
   - 应该与数据库中的数据一致

### 7.2 完整的数据循环

```
主网数据 → monitor.py → 计算风险
                ↓
        ┌───────┴───────┐
        ↓               ↓
   PostgreSQL      Sepolia合约
        ↓               ↓
    API Server      ethers.js
        ↓               ↓
        └───────┬───────┘
                ↓
            React前端
```

## 🎯 常见操作

### 查看API日志

```bash
# API服务器终端会显示所有请求
# 例如：
# INFO: 127.0.0.1:54321 - "GET /api/markets HTTP/1.1" 200 OK
```

### 查看监控日志

```bash
# 监控服务终端会显示：
# - 抓取的交易数
# - 计算的风险评分
# - 上链的交易哈希
```

### 停止所有服务

```bash
# 按 Ctrl+C 停止各个终端的服务

# 停止Docker数据库
docker-compose down
```

### 重启数据库

```bash
# 重启但保留数据
docker-compose restart

# 完全重置（删除所有数据）
docker-compose down -v
docker-compose up -d
# 然后重新初始化schema和测试数据
```

## 🐛 故障排查

### 问题1: API无法连接数据库

**错误信息：**
```
{"status": "unhealthy", "error": "connection refused"}
```

**解决方案：**
```bash
# 检查数据库是否运行
docker ps | grep chainmonitor-db

# 检查.env中的数据库配置
cat .env | grep DB_
```

### 问题2: 前端无法获取数据

**错误信息：**
```
Network Error
```

**解决方案：**
```bash
# 1. 确认API服务器在运行
curl http://localhost:8000/api/health

# 2. 检查CORS配置
# frontend/.env中的VITE_API_BASE_URL应该是 http://localhost:8000/api

# 3. 检查浏览器控制台是否有跨域错误
```

### 问题3: 监控服务连接不上主网

**错误信息：**
```
RuntimeError: 无法连接 mainnet 节点
```

**解决方案：**
```bash
# 检查.env中的ETH_RPC_URL配置
cat .env | grep ETH_RPC_URL

# 测试RPC连接
curl -X POST $ETH_RPC_URL \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}'
```

### 问题4: 合约交易失败

**错误信息：**
```
insufficient funds for gas
```

**解决方案：**
```bash
# 1. 确保你的钱包在Sepolia测试网有ETH
# 访问 https://sepoliafaucet.com/ 获取测试ETH

# 2. 检查私钥配置
# .env中的PRIVATE_KEY应该对应有余额的钱包
```

## 📚 进阶配置

### 自定义监控频率

编辑 `backend/monitor.py` 第260行：

```python
if __name__ == "__main__":
    monitor_loop(
        poll_interval=120,  # 改为120秒（2分钟）一次
        blocks_back=5000    # 回溯5000个区块
    )
```

### 添加新市场

1. 在 `backend/markets.json` 添加配置
2. 在数据库中插入记录：
   ```sql
   INSERT INTO markets (market_id, label, market_type, contract_address, ...)
   VALUES ('NEW_MARKET', 'My New Market', 'dex_pool', '0x...', ...);
   ```
3. 重启监控服务

### 修改风险评分算法

编辑 `backend/monitor.py` 的 `compute_risk_level()` 函数（第77-149行）。

## 🎉 成功运行检查清单

- [ ] PostgreSQL数据库运行中（docker ps）
- [ ] 数据库有测试数据（SELECT COUNT(*) FROM markets）
- [ ] API服务器运行在8000端口（http://localhost:8000/docs）
- [ ] 后端监控服务在抓取数据（终端有日志输出）
- [ ] 前端应用运行在5173端口（http://localhost:5173）
- [ ] 前端能显示市场列表（来自API）
- [ ] 智能合约部署在Sepolia（Etherscan可查）
- [ ] 监控服务能成功调用合约（Etherscan有交易记录）

## 🔗 相关链接

- Sepolia Faucet: https://sepoliafaucet.com/
- Infura RPC: https://infura.io/
- Sepolia Etherscan: https://sepolia.etherscan.io/
- API文档: http://localhost:8000/docs

---

**现在你已经完整运行了ChainMonitor全栈应用！** 🚀
